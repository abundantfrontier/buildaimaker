import SwiftUI

/// Shared semantic colors for the BuildAIMaker shell.
public enum BAMColors {
    public static let sidebarBackground = Color(nsColor: .controlBackgroundColor)
    public static let detailBackground = Color(nsColor: .windowBackgroundColor)
    public static let accent = Color.accentColor
    public static let secondaryLabel = Color(nsColor: .secondaryLabelColor)
    public static let tertiaryLabel = Color(nsColor: .tertiaryLabelColor)
    public static let separator = Color(nsColor: .separatorColor)
}
